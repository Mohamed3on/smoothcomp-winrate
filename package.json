{
  "name": "smoothcomp-winrate",
  "private": true,
  "description": "Tooling only. The extension itself ships as plain script tags with no build step and no dependencies.",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  }
}
